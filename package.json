{
  "name": "hammy-focus-house-cloud",
  "version": "25.6.0",
  "private": true,
  "description": "Hammy Focus House v25.6 with a saved six-language mode and live translation across the game, cloud account, and Room Designer",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check cloud.js && node --check online.js && node --check gallery.js && node --check admin.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}