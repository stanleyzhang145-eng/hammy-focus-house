{
  "name": "hammy-focus-house-cloud",
  "version": "24.7.0",
  "private": true,
  "description": "Hammy Focus House v24.7 with verified admin gifts applied to both the running game state and cloud save",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check cloud.js && node --check online.js && node --check gallery.js && node --check admin.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}