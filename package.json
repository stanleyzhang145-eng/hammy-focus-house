{
  "name": "hammy-focus-house-cloud",
  "version": "25.4.0",
  "private": true,
  "description": "Hammy Focus House v25.4 with layered body-fitted clothing that adapts to every breed and visual style",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check cloud.js && node --check online.js && node --check gallery.js && node --check admin.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}